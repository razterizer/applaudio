@PACKAGE_INIT@

#  Disclaimer: AI-generated script. Not tested at all.

include(CMakeFindDependencyMacro)

# Handle platform-specific dependencies
if(APPLE)
    # macOS frameworks are handled by CMake automatically
    
elseif(WIN32)
    # ole32 is standard Windows library
    
elseif(UNIX AND NOT APPLE)
    find_dependency(PkgConfig REQUIRED)
    pkg_check_modules(ALSA REQUIRED alsa)
    
endif()

include("${CMAKE_CURRENT_LIST_DIR}/applaudioTargets.cmake")

# Check that all required system libraries are available
check_required_components(applaudio)
